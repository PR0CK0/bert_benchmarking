# BERT Benchmarking - Cache Configuration
# Copy this file to .env and customize for your system
# Models will download to these locations (can be 10-50GB total)

# =============================================================================
# IMPORTANT: Choose a drive with sufficient space (20-50GB recommended)
# =============================================================================

# Windows examples:
# BERT_CACHE_DIR=C:\bert_cache
# BERT_CACHE_DIR=D:\ml_cache
# BERT_CACHE_DIR=E:\models\cache

# Linux/Mac examples:
# BERT_CACHE_DIR=/mnt/data/bert_cache
# BERT_CACHE_DIR=/home/username/.cache/bert
# BERT_CACHE_DIR=/media/external_drive/ml_cache

# Default (uncomment and modify):
BERT_CACHE_DIR=./cache

# =============================================================================
# Advanced: Override specific cache locations (optional)
# If not set, these will use subdirectories of BERT_CACHE_DIR
# =============================================================================

# Hugging Face model cache
# HF_HOME=${BERT_CACHE_DIR}/huggingface

# Transformers specific cache
# TRANSFORMERS_CACHE=${BERT_CACHE_DIR}/transformers

# Datasets cache
# HF_DATASETS_CACHE=${BERT_CACHE_DIR}/datasets

# Torch hub cache (for PyTorch models)
# TORCH_HOME=${BERT_CACHE_DIR}/torch

# =============================================================================
# Notes:
# - Use forward slashes (/) or double backslashes (\\) on Windows
# - Relative paths are relative to the project directory
# - Absolute paths recommended for clarity
# - Don't put this in OneDrive/Dropbox/Google Drive folders
# =============================================================================
