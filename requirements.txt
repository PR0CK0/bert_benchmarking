# NOTE: Install PyTorch 2.6+ separately based on your system
# PyTorch 2.6+ required to fix CVE-2025-32434 security vulnerability
# This allows loading all 16 model formats including legacy pickle-based models
#
# WINDOWS: Just run fix_pytorch.bat
#
# LINUX/MAC - Use the CUDA version that matches your system:
# For CUDA 12.6 (RTX 40xx series, newest):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# For CUDA 12.4 (RTX 30xx/40xx series):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# For CUDA 11.8 (older GPUs):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# For CPU only:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# torch>=2.6.0  # Commented out - install separately using commands above

# Core dependencies
numpy>=1.24.0  # PyTorch 2.6+ supports both numpy 1.x and 2.x
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0
psutil>=5.9.0
nvidia-ml-py3>=7.352.0
pyyaml>=6.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
python-dotenv>=1.0.0
protobuf>=3.20.0,<5.0.0  # Required for DeBERTa models
tabulate>=0.9.0
sentencepiece>=0.1.99  # Required for some models
